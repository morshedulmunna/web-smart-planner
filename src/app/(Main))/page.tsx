import React, { type FC } from 'react';

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <React.Fragment>
      <div className="h-screen bg-white">timeline</div>
    </React.Fragment>
  );
};
export default page;
